import { StyleSheet } from 'react-native'

export const sharedStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff'
  },
  containerDark: {
    flex: 1,
    backgroundColor: '#000'
  }
})
